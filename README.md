# AI Video Generation Pipeline

Automated AI pipeline that:
- Generates script using Gemini
- Creates SEO metadata
- Generates thumbnail
- Creates AI voice
- Generates final video

## Setup

pip install -r requirements.txt

Create .env file based on .env.example

Run:
python main.py
